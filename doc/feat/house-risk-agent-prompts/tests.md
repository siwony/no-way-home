# Tests

## Unit Tests

다음 항목은 단위 테스트로 검증한다.

- 전세가율 계산
- 총 위험 노출 비율 계산
- 보수적 회수 시뮬레이션 계산
- 말소된 등기 권리 제외
- 접수일자 기준 선순위 권리 판단
- `CRITICAL > DANGER > CAUTION > SAFE` 위험도 병합 우선순위
- 시세 정보 누락 시 `CAUTION` 사유 생성
- 등기부등본 누락 시 핵심 판정 불가 사유 생성
- 음수 금액 방어 로직

## Integration Tests

다음 항목은 Spring Boot 통합 테스트로 검증한다. 기술 스택 연결과 migration 검증은 `doc/init/tech-stack.md`의 초기화 테스트에서 먼저 통과해야 한다.
- 진단 요청 생성
- 등기부등본 파일 업로드
- 건축물대장 파일 업로드
- 시세 정보 저장
- 위험도 분석 실행
- 리포트 조회
- 체크리스트 조회
- 본인 소유가 아닌 진단 요청 접근 차단

## Playwright E2E Checklist

Playwright 테스트를 작성할 때는 `skills/playwright-e2e/SKILL.md`를 먼저 읽고 기능 체크리스트를 만든다.

E2E 체크리스트는 다음 흐름을 포함한다.

- 사용자가 진단 요청을 생성한다.
- 필수 입력값 누락 시 검증 오류를 확인한다.
- 등기부등본과 건축물대장 파일을 업로드한다.
- 시세 정보를 입력하거나 저장한다.
- 분석 실행 후 위험도 리포트를 조회한다.
- 위험 사유, 추가 확인 항목, 체크리스트가 표시된다.
- 권한이 없는 사용자는 리포트를 조회할 수 없다.

## Final Quality Checklist

- [ ] `doc/init/tech-stack.md`의 초기 스택 전제가 충족되어 있는가?
- [ ] 등기부등본 분석 항목이 포함되어 있는가?
- [ ] 건축물대장 분석 항목이 포함되어 있는가?
- [ ] 시세 기반 계산식이 포함되어 있는가?
- [ ] 위험도 등급 룰셋이 명확한가?
- [ ] 사용자 리포트 예시가 있는가?
- [ ] DB 설계가 있는가?
- [ ] Kotlin 도메인 모델이 있는가?
- [ ] REST API 설계가 있는가?
- [ ] MVP 구현 단계가 있는가?
- [ ] 개인정보/보안 설계가 있는가?
- [ ] “안전하다” 같은 단정 표현을 피했는가?
