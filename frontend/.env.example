VITE_API_BASE_URL=/api
# Optional: force a single backend origin instead of auto-detection.
# VITE_BACKEND_ORIGIN=http://localhost:8080
# Optional: comma-separated candidates to probe when VITE_BACKEND_ORIGIN is not set.
# VITE_BACKEND_CANDIDATES=http://localhost:8080,http://127.0.0.1:8081
